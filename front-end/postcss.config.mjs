const config = {
   theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-montserrat)', 'sans-serif'],
      },
    },
  },
  
  plugins: ["@tailwindcss/postcss"],
};

export default config;
