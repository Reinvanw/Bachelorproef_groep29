
- name: PING
  hosts: switch
  gather_facts: False
  collections:

  vars_files:


  tasks:
    - name: Run show version
      ansible.builtin.wait_for_connection:
        

    - name: print output
      debug:
        var: output.stdout_lines